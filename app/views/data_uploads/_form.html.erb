<%= form_with(model: data_upload) do |form| %>
  <% if data_upload.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(data_upload.errors.count, "error") %> prohibited this data_upload from being saved:</h2>

      <ul>
        <% data_upload.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :name, class: "form-label" %>
    <%= form.text_field :name, class: "form-control", placeholder: "Enter the name of the section data upload" %>
  </div>

  <div class="mb-3">
    <%= form.label :reporting_period_start, class: "form-label" %>
    <%= form.date_field :reporting_period_start, class: "form-control" %>
  </div>

  <div class="mb-3">
    <%= form.label :reporting_period_end, class: "form-label" %>
    <%= form.date_field :reporting_period_end, class: "form-control" %>
  </div>

  <div class="d-flex flex-row-reverse">
    <%= form.submit class: 'btn btn-success' %>
  </div>
<% end %>
