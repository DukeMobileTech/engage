<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Engage" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= favicon_link_tag asset_path("favicon.ico") %>
  </head>

  <body>
    <div class="container">
      <header class="d-flex flex-wrap align-items-center justify-content-center justify-content-md-between py-3 mb-4 border-bottom">
        <div class="col-md-3 mb-2 mb-md-0">
          <%= link_to image_tag("logo.png"), root_path %>
        </div>
        <ul class="nav col-12 col-md-auto mb-2 justify-content-center mb-md-0">
          <li><%= link_to "Sites", sites_path, class: "nav-link px-2" %></li>
          <li><%= link_to "Participants", participants_path, class: "nav-link px-2" %></li>
          <li><%= link_to "Data Exports", data_uploads_path, class: "nav-link px-2" %></li>
          <li><%= link_to "Curriculums", curriculums_path, class: "nav-link px-2" %></li>
          <li><%= link_to "Survey Forms", questionnaires_path, class: "nav-link px-2" %></li>
          <li><%= link_to "Users", users_path, class: "nav-link px-2" %></li>
        </ul>
        <div class="col-md-3 text-end">
          <% if authenticated? %>
            <span class="me-2">Welcome, <strong><%= Current.user.name %></strong></span>
            <%= link_to "Sign Out", session_path, "data-turbo-method": :delete, class: "btn btn-outline-danger" %>
          <% else %>
            <%= link_to "Sign In", new_session_path, class: "btn btn-outline-primary me-2" unless current_page?(new_session_path) %>
          <% end %>
        </div>
      </header>
    </div>
    <main class="container">
      <%= yield %>
    </main>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
  </body>
</html>
