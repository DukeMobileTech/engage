<div class="mb-5">
  <% content_for :title, "Editing organization" %>

  <h4 class="text-center">Editing organization</h4>

  <%= render "form", organization: @organization %>

  <div>
    <%= link_to "Show this organization", @organization, class: "btn btn-secondary" %>
  </div>
</div>
