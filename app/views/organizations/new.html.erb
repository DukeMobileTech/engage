<div class="mb-5">
  <% content_for :title, "New organization" %>
  <h4 class="text-center">New organization</h4>

  <div class="row">
    <%= render "form", organization: @organization %>
  </div>

  <div>
    <%= link_to "Back to organizations", organizations_path, class: "btn btn-secondary" %>
  </div>
</div>