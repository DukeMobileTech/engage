<div class="mb-5">
  <p style="color: green"><%= notice %></p>
  <% content_for :title, "New site" %>
  <h4 class="text-center">New site</h4>

  <div class="row">
    <%= render "form", site: @site %>
  </div>
</div>