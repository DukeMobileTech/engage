<div class="card">

  <% content_for :title, "New site" %>

  <div class="card-header">
    <h3>Creating New Site</h3>
  </div>

  <div class="card-body">
    <%= render "form", site: @site %>
  </div>

  <div class="m-5">
    <%= link_to "Back to sites", sites_path, class: 'btn btn-primary' %>
  </div>
</div>
