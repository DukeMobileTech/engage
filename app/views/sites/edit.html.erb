<div class="card">
  <% content_for :title, "Editing site" %>

  <div class="card-header">
    <h3>Editing Site</h3>
  </div>

  <div class="card-body">
    <%= render "form", site: @site %>
  </div>

  <div class="m-1">
    <%= link_to "Show this site", @site, class: 'btn btn-primary' %>
  </div>

</div>
