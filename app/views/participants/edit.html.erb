<div class="row">
  <% content_for :title, "Editing participant" %>
  <h4 class="text-center">Editing Participant</h4>
  <%= render "form", participant: @participant %>

  <div class="m-1">
    <%= link_to "Show this participant", participant_path(@participant), class: "btn btn-primary" %>
  </div>
</div>